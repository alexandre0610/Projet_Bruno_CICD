info:
  name: get log out
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}users/logout"
  auth:
    type: bearer
    token: "{{acces_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
